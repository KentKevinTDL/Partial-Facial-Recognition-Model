# Partial-Facial-Recognition-Model
A PyTorch-based deep learning model for robust identity verification using partial facial features. Handles occlusions (masks, shadows) by focusing on visible segments (eyes, forehead). Trained with augmentation (Albumentations), uses RetinaFace for detection, FAISS for efficient matching, and includes a Streamlit demo.
